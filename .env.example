# Required by /api/community-join (the "Join Internet Athi Community" signup).
# Server-side only — do NOT prefix these with VITE_, which would bundle them into the
# public frontend. Set the real values in the Vercel project's Environment Variables settings.

# Project URL, e.g. https://xxxxxxxxxxxx.supabase.co
SUPABASE_URL=

# The Supabase project's "service_role" secret key (Project Settings > API).
# This key bypasses Row Level Security — it must only ever be used server-side.
SUPABASE_SERVICE_ROLE_KEY=

# Artist desk and saved booking enquiries. Server only; never use VITE_ prefixes.
# Exact origin, no trailing slash. Use http://localhost:3000 with `vercel dev` locally.
APP_ORIGIN=
# Comma-separated UUIDs from Supabase Authentication > Users. No public admin signup.
ARTIST_ADMIN_USER_IDS=

# Optional future Bandsintown connection. Read-only preview in the artist desk.
BANDSINTOWN_APP_ID=
BANDSINTOWN_ARTIST_ID=
